# AI 味雷达 (AI Flavor Radar)

> 只标不改的 AI 文本检测工具——告诉你哪里有 AI 味，但绝不替你动笔。

## 这是什么

一个纯 Python 的命令行工具，扫描文本中的「AI 味」特征并给出定位报告。**只检测、不改写**——把你该改的地方标出来，怎么改你自己决定。

## 为什么需要它

市面上的「去 AI 味」工具分两类：

| 类型 | 代表 | 问题 |
|------|------|------|
| 检测端 | GPTZero、DetectGPT | 只告诉你「这段像 AI 写的」，不告诉你**哪里**像 |
| 改写端 | 各种「降 AI 率」工具 | 直接帮你改，但经常把正式文档改成大白话，专业术语也给你「口语化」了 |

**AI 味雷达**填补中间空白：

- ✅ 精确到**哪句话、哪个词**有 AI 味
- ✅ 标注**AI 味类型**（骈文 / 套话 / 对仗 / 空洞承诺……）
- ✅ 给出**修改方向建议**，但不替你改
- ✅ 区分场景：投标方案模式 vs 自媒体模式，不同场景规则不同
- ✅ **行业术语白名单**：投标方案里的「服务质量」「管理体系」不算 AI 味

## 快速开始

### 安装

```bash
git clone https://gitee.com/your-username/ai-flavor-radar.git
cd ai-flavor-radar
```

零依赖，Python 3.8+ 即可运行。

### 使用

```bash
# 检测投标方案
python ai_flavor_radar.py your_bid.txt --mode bid

# 检测自媒体文章
python ai_flavor_radar.py your_article.txt --mode social

# JSON 格式输出（方便程序处理）
python ai_flavor_radar.py your_file.txt --mode bid --format json

# 关闭彩色输出（CI/CD 环境）
python ai_flavor_radar.py your_file.txt --mode bid --no-color
```

### 输出示例

```
═══════════════════════════════════════════════
  AI 味检测报告
  文件: examples/sample_bid.txt
  模式: 投标方案
  评分: 100/100  🔴 重度 AI 味
═══════════════════════════════════════════════

命中 23 条 AI 味特征:

[致命] 第 1 段
  类型: 骈文·假大空排比
  原文: 我们将为客户提供高效、专业、优质的全方位服务保障
  建议: 去掉排比形容词堆砌，用具体数字替代「高效专业优质」

[高] 第 2 段
  类型: 空洞承诺
  原文: 建立完善的考核机制
  建议: 补充考核指标、频次、奖惩措施等具体内容
  ...
```

## 检测规则

### 通用规则（common.json）

所有场景都检测的 AI 味特征：

| 规则 | 检测内容 | 示例 |
|------|----------|------|
| C01 | 起承转合套话 | 「众所周知」「不可否认的是」「值得注意的是」 |
| C02 | AI 自我暴露 | 「作为一个 AI」「根据我的分析」 |
| C03 | 万能助词 | 「的情况下」「的过程中」（过度使用） |
| C04 | 对仗 AI 招牌句式 | 「X 而非 Y」「适合 X 不适合 Y」 |
| C05 | 清单符号陷阱 | ①②③ 跨段重复使用 |

### 投标方案专项（bid.json）

| 规则 | 检测内容 |
|------|----------|
| B01 | 骈文·假大空排比（「高效、专业、优质」三连） |
| B02 | 骈文·三段式口号（「以 X 为引领，以 Y 为支撑，以 Z 为保障」） |
| B03 | 空洞承诺（「建立完善的管理机制」但没说怎么建） |
| B04 | 长难句（单句超过 60 字 / 3 个以上逗号 / 3 个以上「的」） |
| B05 | 过度结构化（连续「首先…其次…再次…最后」） |
| B06 | 行业术语误判白名单（投标方案中「服务质量」「管理体系」不算 AI 味） |

### 自媒体专项（social.json）

| 规则 | 检测内容 |
|------|----------|
| S01 | 书面语过重（自媒体不该用「笔者认为」「不可否认」） |
| S02 | 段落过长（自媒体段落超过 5 行） |
| S03 | 万能转折堆叠（连续使用「然而」「不过」「但是」） |
| S04 | 总结句模板（「综上所述」「总而言之」收尾） |

## 评分机制

| 评分 | 等级 | 含义 |
|------|------|------|
| 0-20 | 🟢 健康 | 基本无 AI 味 |
| 21-50 | 🟡 轻度 | 有少量 AI 味，局部修改即可 |
| 51-80 | 🟠 中度 | AI 味较明显，需要重点修改 |
| 81-100 | 🔴 重度 | AI 味浓重，建议通盘重写 |

评分 = min(100, 命中数 × 权重系数)，致命级规则权重最高。

## 项目结构

```
ai-flavor-radar/
├── ai_flavor_radar.py    # 核心检测脚本（零依赖）
├── rules/
│   ├── common.json       # 通用规则（5 条）
│   ├── bid.json          # 投标方案专项规则（6 条）
│   └── social.json       # 自媒体专项规则（4 条）
├── examples/
│   ├── sample_bid.txt    # 投标方案示例（检测演示用）
│   └── sample_social.txt # 自媒体示例
├── tests/
│   └── test_basic.py     # 基础测试
├── README.md
├── LICENSE
└── requirements.txt      # 空（零依赖）
```

## 自定义规则

规则文件是 JSON 格式，你可以自由编辑：

```json
{
  "id": "C01",
  "name": "起承转合套话",
  "severity": "high",
  "patterns": ["众所周知", "不可否认"],
  "suggestion": "直接删掉，或用具体事实替代"
}
```

- `severity`: `fatal` / `high` / `medium` / `low`
- `patterns`: 字符串列表，文本中出现即命中
- `suggestion`: 修改方向建议（只建议，不替你改）

## 适用场景

- ✅ 投标方案自审（提交前扫一遍 AI 味）
- ✅ 公众号文章去 AI 味（发布前检测）
- ✅ 论文/报告自查（避免被判定为 AI 生成）
- ✅ 团队协作（给同事的文章标 AI 味，比口头说「感觉像 AI 写的」更具体）

## 不适用场景

- ❌ 检测学生是否用 AI 写作业（本工具检测的是文风特征，不是 AI 生成检测）
- ❌ 自动改写文本（本工具只标不改）
- ❌ 替代人工审稿（AI 味只是文风问题之一，内容质量仍需人工判断）

## 技术特点

- **零依赖**：纯 Python 标准库，`git clone` 下来直接 `python3` 就能跑
- **规则外置**：所有检测规则在 JSON 文件中，改规则不用改代码
- **多模式**：`--mode bid` / `--mode social`，不同场景不同规则集
- **多格式输出**：终端彩色 / JSON / Markdown

## License

MIT License - 随便用，改了记得留个出处。
